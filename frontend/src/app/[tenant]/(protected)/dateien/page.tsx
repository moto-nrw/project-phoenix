import { FilesPage } from "~/components/files/files-page";

// Dateiablage (#2596): folders with a visibility rule and the files inside.
export default function DateienPage() {
  return <FilesPage />;
}
