meta {
  name: Resolve Tenant
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/tenant/resolve?slug=t1
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: isNumber
  res.body.slug: isString
  res.body.name: isString
}
