meta {
  name: Auth Example
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@example.com",
    "password": "Test1234%"
  }
}

docs {
  # Authentication Example
  
  Shows how to authenticate with the API.
  
  **Response:**
  ```json
  {
    "access_token": "eyJ...",
    "refresh_token": "eyJ..."
  }
  ```
  
  **Usage:**
  Use the access_token in Authorization header:
  `Authorization: Bearer <access_token>`
}