{
  "schema_version": 2,
  "ticket_kind": "migration",
  "checkpoint_reference": "https://github.com/moto-nrw/project-phoenix/issues/3020",
  "prerequisites": ["#2715"],
  "owner_and_capability": "Required ownership splits — Backfill Membership and Workforce data from users.staff; School Membership owns users.staff_school_memberships, Workforce owns users.staff_employment_profiles, migrations own platform.storage_backfill_checkpoints",
  "affected_packages": ["database/migrations", "cmd"],
  "affected_tables": ["users.staff", "users.staff_school_memberships", "users.staff_employment_profiles", "platform.storage_backfill_checkpoints"],
  "exact_ratchet_keys": [],
  "atomic_cutover": "No caller switch. Migration 1.15.382 creates the checkpoint table and copies users.staff per school in id-ordered batches of 500 with a persisted high-water mark; each batch is one REPEATABLE READ transaction that upserts memberships and profiles by preserved identity and commits independently. A Staff-specific advisory session lock excludes concurrent run/reset/down. Changed old rows are re-read until a pass changes nothing; counts, SHA-256 checksums, mismatches and verification_snapshot are persisted in one UTC REPEATABLE READ verification transaction. The stable checkpoint is the final-delta input for Cutover #2753. No dual write is introduced.",
  "tests": ["database/migrations: interrupt and resume after every batch boundary, rerun of completed batches, injected 40P01/40001/55P03 with retry accounting and give-up, changed-row re-read plus orphan removal, foreign work-time model rejection, mid-pass rejoin unique-conflict restart, two-tenant RLS reads and checkpoint grant denial, index validity and index-backed batch plan with sequential scans disabled, reset, guarded down/up with the Expand rollback and post-Cutover view guard. cmd: run, status, reset and the unstable non-zero exit against an isolated clone."],
  "runtime_evidence": {
    "source": "docs/operations/staff-owner-storage-backfill.md (local 2026-09-10 migration and CLI test runs; staging record pending)",
    "workload": "Local isolated PostgreSQL clones per test with two schools, up to seven staff rows per school covering every copied column, batch sizes 1 to 10, passes until stable. Staging run with the previous image serving staff traffic is pending.",
    "thresholds": "No latency SLO. Acceptance is per tenant: source_count = target_count, equal canonical checksums, mismatch_count 0, rows_rejected explained, stable = true. The command exits non-zero otherwise.",
    "query_count": "Per attempt: worker PID lookup, one SET LOCAL statement, checkpoint row lock, combined copy CTE, checkpoint update; separate observer samples pg_stat_activity every 10 ms. Per pass end: orphan delete transaction then one UTC REPEATABLE READ verification transaction containing SET LOCAL, two checksum queries, mismatch query, snapshot/time query and checkpoint update. Per run: advisory acquire/release, school listing, sequence alignment and status reads. Terminal failure adds one bounded telemetry-only update.",
    "errors": "Zero errors in the local suites. Non-transient SQLSTATEs abort the run immediately with the committed checkpoint intact; transient classes retry up to five attempts and then fail with the attempt count.",
    "failure": "Injected deadlock, serialization failure and lock timeout roll back the batch without double counting rows; the attempt cap surfaces 'gave up after N attempts'. A foreign work-time model reference is rejected, reported as mismatch with oldest_unmigrated_at, and keeps the tenant unstable.",
    "rollback": "Each interrupted batch rolls back to its previous checkpoint; resume continues exactly one batch further per run in the interrupt test. Reset and the migration down step truncate only target rows and this backfill's checkpoints, keeping other backfills' rows in the shared table; users.staff row JSON is byte-identical before and after. Both refuse when users.staff is a view.",
    "smoke": "scripts/test-changed.sh origin/development passed for cmd, database/migrations, internal/architecture, seed/api and test; migrate validate passed; architecture check passed with composition 796 -> 796 and 1,647 remaining legacy violations; golangci-lint zero issues; lefthook pre-commit passed. Recorded in docs/operations/staff-owner-storage-backfill.md.",
    "affected_rows": "Local: rows_copied equals the source count on the first pass, zero on the stabilising pass; rows_removed equals physically deleted source rows. Staging per-tenant totals pending.",
    "lock_wait": "Worker pg_stat_activity Lock waits are sampled on an independent connection every 10 ms and accumulated per tenant as lock_wait_ms, including successful sub-timeout waits and failed attempts. Sampling errors fail the batch. lock_timeout remains 5s; external staging observer corroboration is pending.",
    "deadlocks": "Every 40P01 failed attempt is counted per tenant, including terminal exhaustion. Pending telemetry is flushed on terminal failure without advancing the data checkpoint; abrupt process death or unavailable database can prevent the bounded flush.",
    "pool_wait": "Pool-wide sql.DBStats wait duration observed while each tenant's batches ran is persisted as pool_wait_ms (zero locally); it is an upper bound that includes concurrent users of the shared process pool.",
    "job_duration": "batch_p95_ms and batch_max_ms are persisted per tenant; local batches complete in single-digit milliseconds. Staging migration wall time pending.",
    "job_retries": "batches_retried counts actual retry starts; deadlocks, serialization_failures and lock_timeouts count all failed attempts. Successful batches persist pending counters atomically with progress; terminal failures flush counters separately with a 5s bound and preserve persistence errors. CLI prints available partial reports before returning errors.",
    "job_backlog": "oldest_unmigrated_at records the oldest differing source row at verification; null once stable."
  },
  "rollback_and_cleanup": "Stop the run and keep the checkpoint. Before Cutover, `backfill staff-owner reset` or migration down truncates only the target rows and checkpoints and restarts from zero; the old authoritative rows are never mutated or deleted. Contract of users.staff happens after the Cutover rollback window, not here.",
  "exit_criterion": "Every school reports stable = true with equal counts and checksums, zero mismatches and orphans, and a complete checkpoint while users.staff remains authoritative. scripts/backend-architecture.sh check introduces no key and leaves policy ownership unchanged; the new checkpoint table is owned by migrations."
}
