// Package staff exposes the School Membership HTTP adapter under /api/staff.
// It talks to the public capability only; rendering, auth and every foreign
// lookup are injected by the root.
package staff
