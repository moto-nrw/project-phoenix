# BetterAuth Service Environment Variables
# Copy to .env and fill in actual values

# =============================================================================
# Database Configuration (same PostgreSQL as Go backend, SSL required)
# =============================================================================
DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres?sslmode=require

# =============================================================================
# BetterAuth Configuration
# =============================================================================
# Secret key for JWT and session encryption (minimum 32 characters)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-minimum-32-characters-long

# Base URL where BetterAuth service is accessible
BETTER_AUTH_URL=http://localhost:3001

# Trusted origins for CSRF protection (comma-separated)
# Include frontend URL and any other services that need auth
TRUSTED_ORIGINS=http://localhost:3000,http://localhost:8080

# =============================================================================
# Email Configuration
# =============================================================================
# Internal API URL for sending emails via Go backend (Docker internal network)
INTERNAL_API_URL=http://server:8080

# Base domain for constructing subdomain URLs in emails (required, no fallback)
# Examples: moto-app.de (production), localhost:3000 (development)
BASE_DOMAIN=moto-app.de

# =============================================================================
# Server Configuration
# =============================================================================
PORT=3001
