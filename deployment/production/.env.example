# Production Caddy Server Configuration Environment Variables
# Copy this file to .env in the deployment/production directory
# This file is used ONLY by the Caddy server in production
# It's separate from the main production .env file

# Email for Let's Encrypt certificates
ADMIN_EMAIL=your-email@example.com

# Primary domain (frontend)
DOMAIN=moto-app.de

# API subdomain
API_DOMAIN=api.moto-app.de

# Optional: Caddy admin API key (for remote administration)
# CADDY_ADMIN_KEY=your-secure-api-key

# Optional: Custom log directory
# LOG_DIR=/var/log/caddy